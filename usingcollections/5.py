"cat"

(3, 1, 4, 1, 5, 9, 2)
["a", "b"]  # Cannot be used as key since list is mutable.
{"a": 1, "b": 2}  # Cannot be used as key since dict is mutable.
range(5)
{1, 4, 9, 16, 25}  # Cannot be used as key since set is mutable.
3
0.0
frozenset({1, 4, 9, 16, 25})
